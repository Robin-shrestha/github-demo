services:
  app:
    build: .
    ports:
      - "3001:3001"
    env_file: .env
    environment:
      # Override localhost → host machine so the container can reach your local MongoDB.
      # host.docker.internal is resolved by Docker Desktop on Mac/Windows.
      MONGO_URI: mongodb://host.docker.internal:27017
      PORT: 3001
